# romanantl.cz
Personal website built with Astro (prerendered pages and a Node server for the contact API), React islands, and Tailwind CSS. Production runs in Docker (see `Dockerfile` and `docker-compose.yml`).
